@model IEnumerable<GameCenterMVC.Models.Roles>

@{
	ViewBag.Title = "Index";
	Layout = "~/Views/Shared/_DashboardLayout.cshtml";
}

<link href="~/Content/Table.css" rel="stylesheet" />
@*<link href="~/Content/TableTest.css" rel="stylesheet" />*@
<h2>Roles Data Manger</h2>
<th>
	@*@Html.ActionLink("Create New", "Create")*@
	<a href="@Url.Action("Create","Role")" @*asp-action="Create"*@ class="btn btn-primary text-white"> <i class="fa fa-plus-circle"></i> New Role</a>
</th>
@*<p>
		@Html.ActionLink("Create New", "Create")
	</p>*@
<table class="container">
	<thead>
	<tr class="header">
		<th>
			@Html.DisplayNameFor(model => model.InsertBy)
		</th>
		<th>
			@Html.DisplayNameFor(model => model.InsertDate)
		</th>
		@*<th>
				@Html.DisplayNameFor(model => model.UpdateBy)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.UpdateDate)
			</th>*@
		<th>
			@Html.DisplayNameFor(model => model.Name)
		</th>
		<th>
			@Html.DisplayNameFor(model => model.Description)
		</th>
		<th></th>
	</tr>

	@foreach (var item in Model)
	{
		<tr>
			<td>
				@Html.DisplayFor(modelItem => item.InsertBy)
			</td>
			<td>
				@Html.DisplayFor(modelItem => item.InsertDate)
			</td>
			@*<td>
					@Html.DisplayFor(modelItem => item.UpdateBy)
				</td>
				<td>
					@Html.DisplayFor(modelItem => item.UpdateDate)
				</td>*@
			<td>
				@Html.DisplayFor(modelItem => item.Name)
			</td>
			<td>
				@Html.DisplayFor(modelItem => item.Description)
			</td>
			<td>
				@Html.ActionLink("Edit", "Edit", new { id = item.RoleID }) |
				@Html.ActionLink("Details", "Details", new { id = item.RoleID }) |
				@Html.ActionLink("Delete", "Delete", new { id = item.RoleID })
			</td>
		</tr>
	}

</table>
